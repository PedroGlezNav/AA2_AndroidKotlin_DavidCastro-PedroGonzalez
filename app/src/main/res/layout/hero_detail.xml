<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@color/nav_bar"
        >
        <ImageButton
            android:id="@+id/hero_detail_arrow"
            android:layout_width="@dimen/image_button_aid"
            android:layout_height="@dimen/image_button_aid"
            android:background="@drawable/baseline_arrow_back_24"
        ></ImageButton>

        <Space
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"/>

        <ImageButton
            android:id="@+id/hero_delete_icon"
            android:layout_width="@dimen/image_button_aid"
            android:layout_height="@dimen/image_button_aid"
            android:background="@drawable/baseline_delete_forever_24"
            ></ImageButton>

    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/padding_normal">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text ="Hero Name"
                android:textAlignment="center"
                android:textSize="@dimen/text_title"
                android:id="@+id/detail_hero_title"
                />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text ="Hero Description"
                android:textSize="@dimen/text_regular"
                android:layout_marginTop="@dimen/padding_normal"
                android:id="@+id/detail_hero_desc"
                />
        </LinearLayout>
    </ScrollView>
</LinearLayout>